# Documentation Review TODO

## Cross-cutting Documentation Work

- Use that page as the main cross-link target from quickstart, setup, chart READMEs, conceptual docs, and troubleshooting pages whenever the docs need to explain Helm-first versus raw CR workflows.

## README.md

- Fix the external Keycloak configuration example to match the actual chart contract, or update it after the chart/schema contract is standardized.

## docs/index.md

- Replace the raw RBAC Role example with Helm-first guidance, or clearly mark it as an advanced/manual path.
- Clarify that "no separate token system" refers to provisioning authorization, not normal OAuth2/OIDC tokens issued to applications.

## docs/quickstart/README.md

- Call out that the chart currently has `enabled`/`managed` drift in supporting templates, because that blocks documentation correctness until resolved.
- Explain that applications should consume the generated credentials Secret directly in-cluster instead of manually reading the secret in the quick start.
- Show at least one concrete example of how to inject the generated Secret into an application Deployment.
- Add a short explanation of how the generated credentials secret name is derived from the client resource name.

## docs/how-to/end-to-end-setup.md

- Rename the docs navigation label from a generic "Installation" to something closer to "End-to-End Setup" or "Production Deployment" so readers do not mistake this longer, infrastructure-heavy guide for the basic install path.

## charts/README.md

- Decide whether this page should move into the MkDocs hierarchy or be reduced to a short GitHub-facing pointer, because it is currently not reachable from the published docs navigation.

## charts/keycloak-operator/README.md

- Expand the production controls section further so it calls out the remaining operational knobs that were not yet covered, especially pause semantics, metrics host/port, and reconciliation timing details.
- Make the one-Keycloak-per-namespace limit explicit in the README instead of leaving it implicit in webhook quota configuration.

## charts/keycloak-realm/README.md

- Add a proper SMTP secret-label example so users know how to grant the operator read access to the SMTP password secret.

## charts/keycloak-client/README.md

- Investigate and fix the `authenticationFlow` versus `authenticationFlows` mismatch across the chart, schema, template, and documentation.

## docs/concepts/architecture.md

## docs/concepts/security.md

## docs/admission-webhooks.md

## docs/how-to/multi-tenant.md

## docs/how-to/database-setup.md

- Review the linked backup and restore flow for token-era drift because it affects the database operations path from this guide.

## docs/guides/identity-providers.md

## docs/guides/user-federation.md

## docs/guides/observability.md

## docs/guides/drift-detection.md

## docs/operations/day-two.md

## docs/operations/secret-management.md

- Fix the Kyverno policy example so it targets the actual secret label used by the operator.
- Add a practical "Using External Secrets Operator (ESO)" section and cover Sealed Secrets as a supported GitOps-friendly pattern.
- Restructure the page into three clear concerns: secret provisioning, automated rotation, and application restart coordination.
- Document the `clientSecret` field for manual pre-existing secret binding and explain its constraints.
- Add a table of the operator-managed secret keys and show how applications should consume them.
- Add a short section explaining rotation scheduling semantics, including timezone behavior and timing edge cases.
- Add the missing note about the secret label required for operator read access where relevant.
- Convert the examples to Helm-first patterns where possible and treat raw CR examples as advanced.
- Consider renaming the page or expanding it so the title matches the actual scope.
- Add a note that operator-level reconciliation pause also pauses secret rotation daemons.
- Add a cross-link from the keycloak-client chart README to this operations guide.

## docs/operations/backup-restore.md

- Remove all `keycloak-operator-token-metadata` ConfigMap references and delete the stale "Token Metadata" row from the backup inventory table.
- Restructure the guide so Helm values and supported operator workflows are the primary path, with raw CR examples moved into an advanced section.
- Add a dedicated section explaining the implemented blue-green upgrade strategy and the relevant state-machine concepts for backup and restore.
- Add a "Database Restore by Tier" subsection covering CNPG, managed, and external database restore expectations separately.
- Add YAML examples that contrast legacy flat-field database config with the recommended explicit tiered configuration.
- Fix the relative links to database-setup and HA deployment docs if they are currently incorrect from this page.
- Add guidance for interrupted blue-green upgrades during disaster recovery.
- Verify the documented label selectors against the real constants and update the monitoring commands if needed.

## docs/operations/migration.md

- Add the migration toolkit workflow as the primary migration path for users coming from the official operator or self-managed Keycloak exports.
- Update the blue-green upgrade section so it describes the implemented feature instead of presenting it as future work.
- Convert the upgrade examples to Helm-first commands, with raw `kubectl patch` examples treated as secondary or advanced.
- Document the cache-isolation priority order and show the relevant Helm values paths.
- Clarify the automatic pre-upgrade backup blocking behavior and the lack of a special backup status phase.
- Qualify the session-loss guidance so readers understand it applies to recreate or rolling behavior, not blue-green cutover.
- Improve the official-operator comparison table to better explain the RBAC and scaling model differences.
- Add a clearer warning about non-semver image tags disabling `autoRevision` isolation.
- Fix the table-of-contents numbering.
- Add cross-references to the migration toolkit guide, escape-hatch guide, and relevant ADRs.

## docs/operations/troubleshooting.md

- Expand the documented phase list so it matches the real set of phases used by the operator, including `Reconciling`, `Paused`, and `BackingUp`.
- Rewrite the admin-console section so it explains that direct UI access is discouraged for drift reasons rather than technically impossible.
- Add explicit version guards wherever management port `9000` is mentioned, with guidance for Keycloak 24.x users.
- Rework troubleshooting fixes so Helm values and Helm upgrades are the primary path, with raw `kubectl patch` commands marked as temporary or advanced.
- Add a section explaining the operator-managed admin credentials secret naming pattern and lifecycle.
- Fix the rate-limiting troubleshooting commands so they use the actual Helm values paths instead of inconsistent env-style fields.
- Add clearer context on why port-forwarding is required for host-side access to cluster-internal services.
- Expand the authorization troubleshooting section to cover missing or incorrect `operatorRef` and namespace-reference mistakes.

## docs/operations/escape-hatch.md

- Reframe the page so it presents supported migration paths without implying that the operator itself is unstable or only suitable for evaluation.
- Reduce and heavily caveat the realm-operator migration path, since it points toward the very design this project was created to replace.
- Replace the hardcoded Keycloak image version in export jobs with a version-detection step and placeholder-based example.
- Add a disclaimer that any realm-operator CRD mapping table must be verified against the target realm-operator version.
- Link the import-users workflow directly to the full migration-toolkit guide.
- Add an export validation step before destructive import or migration work.
- Replace the vague ConfigMap-size warning with concrete recommended alternatives such as PVCs or object storage.
- Reframe the non-Keycloak migration scenario in a less dismissive tone and acknowledge real business constraints.
- Replace the subjective summary-table recommendation column with more neutral descriptors.
- Add bidirectional links with export-realms and migration documentation.

## docs/versioning.md

- Update all version examples so they reflect current real component versions rather than ancient placeholder values.
- Add the migration-toolkit as a first-class independently versioned component throughout the guide.
- Lead with the OCI registry installation method and present the traditional Helm repository flow as an alternative.
- Explain the operator image versus operator chart version divergence with a concrete current example.
- Refresh the version-alignment table using real current values, including chart appVersion where relevant.
- Add clearer links to the versioning guide from quickstart and end-to-end setup docs.
- Add or link to a compatibility matrix so users understand how independently versioned charts relate.
- Investigate why `pyproject.toml` appears out of sync and either fix the release flow or stop implying it is authoritative for users.
- Consider whether this page belongs somewhere more discoverable than the Reference section.

## docs/how-to/ha-deployment.md

- Remove or replace the unsupported `affinity` and `podAntiAffinity` examples, since those fields are not currently part of the Keycloak CRD.
- Rework the guide so Helm-based HA deployment is the primary path and raw CR examples are clearly marked as advanced.
- Add a section explaining automatic JGroups DNS_PING clustering and the Keycloak version requirement for horizontal scaling.
- Replace the raw PDB example with guidance on how to manage disruption budgets in a Helm- or GitOps-friendly way.
- Improve the resource-sizing guidance so it explains the relationship between replicas, JVM settings, and database connection counts.
- Expand the failover tests so they validate real traffic continuity and session behavior, not just pod deletion.
- Add clearer explanation of CNPG replication settings such as `minSyncReplicas` and `maxSyncReplicas`.
- Add missing prerequisites and verification steps for the S3 backup example.
- Add alerting context or recommended thresholds for the monitoring queries.

## docs/faq.md

- Replace the stale `Token-based delegation` wording in the scaling table with the current namespace-grant-based authorization model.
- Fix the supported-version answer to state that Keycloak 24.x is supported, and explain that the operator handles the 24.x versus 25.x+ health-port difference automatically.
- Update `docs/operations/migration.md` in parallel so it does not repeat the same incorrect minimum-version claim.
- Strengthen the GitOps answer by linking directly to the relevant section in `charts/README.md` instead of using a vague repository-level pointer.
- Add a small link from the docs landing page to the FAQ so first-time readers can discover it without hunting through the nav.
- Add an explicit FAQ entry answering why there is no `User` CR, explaining that users are stateful data rather than desired-state configuration and therefore are intentionally not modeled as a reconciled CR.
- Add an explicit FAQ entry answering why the operator exposes `KeycloakRealm` and `KeycloakClient` rather than a larger set of fine-grained CRDs, explaining that supported configuration is either realm-scoped or client-scoped and the API surface is intentionally centered on those two ownership boundaries.
- Remove unsupported scale claims such as the current "real world tested" sentence unless they are backed by documented evidence.

## docs/how-to/migration-toolkit.md

- Add a short "Who Should Read This?" section that distinguishes standalone Keycloak migrations from realm-operator escape-hatch workflows and from existing-operator re-import scenarios.
- Add a version-compatibility note explaining that the toolkit transforms exports generically, but the target operator only supports the Keycloak versions documented in the version-support matrix.
- Add a minimal expected `realm-values.yaml` output example so readers can verify the transformed structure against the current realm chart contract.
- Link the `import-users` behavior and the secret-mode design back to the relevant ADRs so the documented defaults and safety tradeoffs have an authoritative explanation.
- Verify and explicitly reference the current chart field names involved in transformed output and secret behavior so the guide does not drift silently from chart reality.
- Update the Helm install examples to include namespace context.
- Add a note in the full migration example that the realm must already authorize the target client namespaces before the client chart installs will succeed.
- Add or strengthen cross-links from migration and escape-hatch documentation back to this guide where users are likely to look first.

## docs/how-to/export-realms.md

- Add an explicit Helm-first note explaining that the migration toolkit produces Helm values files for the recommended deployment path, not raw CRDs.
- Split the guide more clearly between desired-state configuration export and stateful user/session data export so readers do not confuse backup concerns with GitOps-managed configuration.
- Replace the hardcoded Keycloak `26.0.0` image examples with version-detection guidance and placeholder-based examples.
- Add a prerequisite note and direct link to the migration-toolkit installation steps before the first `keycloak-migrate` command appears.
- Rename or rewrite the import section so it clearly states that the operator manages configuration, while user import uses separate workflows outside normal desired-state reconciliation.
- Tighten the prerequisites for the database-migration path so it is only recommended where the source and target database setup actually make sense.
- Add a bidirectional cross-link with the escape-hatch guide and clarify when this page applies versus when the operator-specific escape-hatch doc applies.
- Make the legacy WildFly guidance explicit about supported target versions and link directly to the version-support documentation.
- Add an export-validation step before destructive follow-up work and a post-import verification step after migration.
- Improve the security of password-handling examples so the guide does not normalize plaintext credentials in shell history.

## docs/how-to/smtp-configuration.md

- Replace every raw CR example that uses `spec.smtp` with the real field path `spec.smtpServer`.
- Replace every `credentialsSecret` example with the real nested `passwordSecret.name` and `passwordSecret.key` structure.
- Update all provider examples and the testing example so they validate against the current realm schema instead of failing on copy-paste.
- Add a clearer explanation of how SMTP credentials map into the spec: username as a normal field, password via secret reference.
- Add a prominent warning that the SMTP password secret must live in the same namespace as the `KeycloakRealm`.
- Add a Helm-versus-raw-CR callout so readers know how the realm chart values map onto the CRD fields.
- Investigate and fix the broader `smtp` versus `smtpServer` drift in chart examples so the page can point at a trustworthy Helm example.
- Move the SSL/TLS port guidance earlier in the guide so users see the common 587/465 configuration split before troubleshooting.

## docs/reference/keycloak-version-support.md

- Keep this page as the single source of truth for supported Keycloak versions and remove conflicting minimum-version claims from other docs by linking back here instead of duplicating tables.
- Convert the ASCII canonical-model architecture diagram to Mermaid.
- Tighten the wording around `keycloakVersion` and semver-tag requirements so it is clear which rules are conditional and which are always enforced.
- Trim or relocate upgrade-strategy and backup-specific content that belongs in migration or operations guides rather than in the version-support reference.
- Add a freshness/source note for the validated-version table and point readers to the authoritative generated data source.
- Add clearer explanation of where version validation failures surface and how users should inspect them.
- Expand the Organizations/version-gated feature notes so readers know which field enables the feature and what version requirement applies.
- Add stronger cross-links from this page to FAQ, migration, and setup docs, and from those docs back to this page.
- Verify and modernize any command examples for model regeneration or version testing so they match current task-based workflows.

## docs/development.md

- Update the architecture diagram so it matches the current repository structure, including the real service, utility, and observability modules, and remove references to nonexistent files.
- Add the repo's explicit warning that `mkdocs serve` is for humans only and should not be used by agents.
- Replace the draft release checklist with a short pointer to the authoritative release process in `RELEASES.md`, or fully align the section if it remains.
- Fix the documented Python version so it matches the current repository requirement.
- Align the testing workflow guidance with the current `task test:all` expectation and link directly to the critical integration-testing rules in `tests/integration/TESTING.md`.
- Add a reference to the decision records as part of the expected contributor workflow.
- Fix minor structural drift such as duplicate step numbering and incomplete example snippets.

## docs/development/testing.md

- Clarify the `task test:integration` lifecycle so readers understand that the task provisions fresh integration infrastructure rather than simply reusing an existing cluster as-is.
- Tighten the cleanup wording so it clearly states what is deleted after a run and what remains until explicit cluster teardown.
- Remove the redundant quick-start comment and clean up any other small duplicated wording.
- Add a short note or cross-reference covering the actual container-based coverage test image path so testing documentation and agent guidance stay aligned.

## docs/rbac-implementation.md

- Fix every use of the secret label so it matches the real required value `vriesdemichael.github.io/keycloak-allow-operator-read`.
- Clarify that the Helm-managed RBAC creation path is the default and explain what changes when `rbac.create` is disabled.
- Add a short explanation that the secret-label requirement is enforced by operator validation, not just recommended by convention.
- Add a stronger cross-link to the multi-tenant guide for readers moving from RBAC concepts to practical namespace setup.
- Consider converting the ASCII architecture diagram to Mermaid while this page is being refreshed.

## docs/decisions/README.md

- Align the documented ADR-inspection command with the authoritative workflow in `AGENTS.md` so contributors and agents are not given two different extraction patterns.
- Expand the template and field reference to cover lifecycle fields such as `status`, `supersedes`, and `superseded_by`.
- Make the task-based validation workflow the primary recommendation and present direct script use as the lower-level alternative.
- Clarify that agent-specific ADR workflow rules live in `AGENTS.md`, while this README is the general contributor-facing field and process reference.
- Document the supporting decision-doc generation scripts so users understand how YAML records become rendered docs.

## docs/reference/keycloak-crd.md

- Expand the spec reference to cover the newer Keycloak fields such as `keycloakVersion`, `optimized`, `operatorRef`, `realmCapacity`, `tracing`, `maintenanceMode`, `cacheIsolation`, and `upgradePolicy`.
- Rewrite the database section around the current tiered architecture instead of treating the legacy flat-field shape as the main model.
- Expand the documented status phases and status fields so backup, pause, capacity, and blue-green upgrade states are represented accurately.
- Add current advanced examples for managed and external databases, blue-green upgrades, realm capacity, tracing, and custom-version images.
- Add cross-links to the ADRs that define the newer database, upgrade, and cache-isolation behavior.
- Resolve any documented default/value ambiguity such as the `optimized` field before presenting defaults as authoritative.

## docs/reference/keycloak-realm-crd.md

- Add missing top-level realm features such as `clientAuthorizationGrants`, password policy, OTP policy, WebAuthn policies, client profiles, client policies, organizations, flow bindings, and required actions.
- Rework the page positioning or wording so it does not claim to be complete while omitting major supported sections.
- Fix field-name drift such as non-canonical authentication execution naming and align examples with the current schema/model terminology.
- Add clear notes for advanced or version-gated features, especially organizations and other security-related configuration areas.
- Add stronger cross-links to the practical guides that explain how multi-namespace authorization and advanced security features are used operationally.

## docs/reference/keycloak-client-crd.md

- Add a full section for fine-grained authorization settings, including resources, scopes, policies, permissions, decision strategy, and enforcement mode.
- Expand the secret-management documentation with the actual `secretRotation` configuration block and scheduling semantics.
- Add at least one advanced example showing a client with authorization settings configured.
- Add fuller prose explanations for URL-related fields and other client settings that currently only appear in tables.
- Clarify any security-sensitive authorization options, such as JavaScript policy support, when documenting the advanced authorization section.
